spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5432/gestao_db
    password: '1234'
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'